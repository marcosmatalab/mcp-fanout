[
  {"tool": "fetch", "arguments": {"url": "https://example.com/"},
   "why": "Baseline egress with no canary, so the destination is measured separately from argument forwarding. url is required and it is the only tool, so no argument-less call is possible."},
  {"tool": "fetch", "arguments": {"url": "https://example.com/search?q=CANARY_QRY_ab12cd34ef56"},
   "why": "The canary rides in the URL, which this server must forward verbatim to do its job. This is the cleanest positive case for number 5: if content matching cannot recover attribution here, it cannot anywhere."}
]
