{
  "attempts": [],
  "capabilities": {
    "experimental": {},
    "tools": {
      "listChanged": false
    }
  },
  "command": [
    "uvx",
    "mcp-server-time@2026.8.18"
  ],
  "env_keys_supplied": [],
  "id": "time",
  "ok": true,
  "protocol_version_used": "2025-11-25",
  "server_info": {
    "name": "mcp-time",
    "version": "1.30.0"
  },
  "server_protocol_version": "2025-11-25",
  "started_without_credentials": true,
  "tool_count": 2,
  "tools": [
    {
      "description": "Get current time in a specific timezone",
      "inputSchema": {
        "properties": {
          "timezone": {
            "description": "IANA timezone name (e.g., 'America/New_York', 'Europe/London'). Use 'Europe/Madrid' as local timezone if no timezone provided by the user.",
            "type": "string"
          }
        },
        "required": [
          "timezone"
        ],
        "type": "object"
      },
      "name": "get_current_time"
    },
    {
      "description": "Convert time between timezones",
      "inputSchema": {
        "properties": {
          "source_timezone": {
            "description": "Source IANA timezone name (e.g., 'America/New_York', 'Europe/London'). Use 'Europe/Madrid' as local timezone if no source timezone provided by the user.",
            "type": "string"
          },
          "target_timezone": {
            "description": "Target IANA timezone name (e.g., 'Asia/Tokyo', 'America/San_Francisco'). Use 'Europe/Madrid' as local timezone if no target timezone provided by the user.",
            "type": "string"
          },
          "time": {
            "description": "Time to convert in 24-hour format (HH:MM)",
            "type": "string"
          }
        },
        "required": [
          "source_timezone",
          "time",
          "target_timezone"
        ],
        "type": "object"
      },
      "name": "convert_time"
    }
  ]
}
